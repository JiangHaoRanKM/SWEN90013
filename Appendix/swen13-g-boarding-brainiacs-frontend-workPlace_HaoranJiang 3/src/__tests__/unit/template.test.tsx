// Replace this import to other modules required
import React from 'react';

describe('Component A', () => {
  beforeEach(() => {});

  afterEach(() => {});

  beforeAll(() => {});

  afterAll(() => {});

  test('should ... when ...', () => {
    expect(true);
  });
});
